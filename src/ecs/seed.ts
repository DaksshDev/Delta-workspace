export const SeedSystem = {
  async seedIfEmpty() {
    // New workspaces should start clean. Study defaults are handled separately
    // by studyStorage.ensureDefaults() where that page needs its folder shell.
  }
};
